// All images in this project are Unsplash URLs — no local asset resolution needed.
// This helper is provided for consistency and future extensibility.
export function imageSrc(value: string): string {
  return value;
}
